<div class="container">
    <div class="row content">
        <div class="col-xs-3">
    <% if flash[:notice1] %>
      <div class="flash">
        <%= flash[:notice1] %>
      </div>
    <% end %>

    <% if flash[:notice2] %>
      <div class="flash">
        <%= flash[:notice2] %>
      </div>
    <% end %>
            <!-- プロフィール -->
            <%= render 'users/info', user: @user %>
            <!-- プロフィール -->

            <!-- 新規投稿 -->

            <%= render 'books/new', book: @book %>

            <!-- 新規投稿 -->

        </div>




            <!-- 投稿一覧画面 -->
        <div class="col-xs-9">
            <h2>Book detail</h2>

                <table class ="table ">

                   <tbody>
                                <tr>
                                    <td>
                                        <%= attachment_image_tag @user, :profile_image, :fill, 50, 50, format: 'jpeg', fallback: "no_image.jpg" %>
                                        <br>
                                        <%= link_to "#{@user.name}", user_path(@user.id) %></td>
                                    <td>
                                        <%= link_to @book_1.title,book_path(@book_1.id)%>
                                    </td>
                                    <td>
                                        <%= @book_1.body %>
                                    </td>

                                        <% if @book_1.user.id == current_user.id %>
                                    <td>
                                        <%= link_to "Edit", edit_book_path(@book_1), class: "btn-sm btn-success" %>
                                    </td>
                                    <td>
                                        <%= link_to "Destroy", book_path(@book_1), method: :delete, class: "btn-sm btn-danger","data-confirm" => "本当に削除しますか？" %>
                                    </td>
                                        <% end %>
                                </tr>

                    </tbody>
                </table>
        </div>
    </div>
</div>